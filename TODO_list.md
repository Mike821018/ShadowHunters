# TODO List

1. 規劃手機版顯示方式（目前手機瀏覽時版面會跑掉）。
	 - 目標：在 360px 到 430px 寬度可正常操作「大廳、房間、回放、紀錄」核心流程。
	 - 階段 A：盤點與截圖
		 - 針對 index、lobby、room、room_preview、replay_room、records 做手機寬度截圖。
		 - 列出每頁的破版元素（溢出、重疊、按鈕不可點、字過小）。
	 - 階段 B：建立響應式規格
		 - 定義 3 個主要斷點：1024、768、480。
		 - 定義最小可操作尺寸（按鈕高度、字級、間距、可點區域）。
		 - 決定房間頁手機版資訊優先順序（先保留回合操作與玩家卡，次要資訊可收合）。
	 - 階段 C：樣式重構
		 - 把固定寬度容器改為流式寬度與最大寬度。
		 - 調整房間頁 grid 佈局為手機單欄，聊天室/玩家卡/操作區改為可折疊區塊。
		 - 修正玩家卡與色盤在手機上的定位、層級與收合互動。
	 - 階段 D：驗證
		 - iPhone 12/13 尺寸、Android 常見尺寸各跑一輪手動流程。
		 - 檢查直式與橫式切換時是否有錯位與操作中斷。
	 - 交付物
		 - 手機版 UI 調整清單
		 - 驗證截圖與問題關閉紀錄

2. 規劃 server 重啟後如何復原現存村莊（目前重開後招募中/遊戲中的房間都會消失）。
	 - 目標：服務重啟後可恢復招募中與進行中的房間狀態，降低玩家中斷。
	 - 階段 A：資料盤點
		 - 定義房間快照必要欄位：room、players、turn、牌堆、待處理事件、聊天與系統訊息。
		 - 區分可重建資料與必須持久化資料。
	 - 階段 B：持久化設計
		 - 新增房間快照表或快照檔案格式（含版本欄位）。
		 - 設計寫入時機：關鍵動作後增量更新 + 週期性快照。
		 - 設計清理策略：已結束房間保留天數與刪除規則。
	 - 階段 C：啟動復原流程
		 - server 啟動時讀取未結束房間快照並重建記憶體狀態。
		 - 加入快照版本相容與容錯（欄位缺漏、舊版本格式）。
		 - 復原失敗時回退安全模式（房間標記失效並提示玩家）。
	 - 階段 D：一致性與壓力驗證
		 - 招募中斷電重啟、遊戲中途重啟、多人同房高頻操作重啟測試。
		 - 驗證復原後回合順序、玩家狀態、牌堆數量與待處理事件一致。
	 - 交付物
		 - 房間狀態持久化規格
		 - 啟動復原流程圖
		 - 重啟復原測試報告
