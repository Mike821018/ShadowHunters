# 請記得修正完成後自行做20回以 HTTP/前端操作路徑的遊戲測試並回填處理結果。
# 請從以下開始填寫，如果有額外的修正項目，請自行新增

### [顯示修正] / BUG / 規則確認 / UX
- 問題標題：Room Preview 玩家卡片暱稱與 TRIP 互相擠壓
- 問題說明：調整 TRIP 位置後，出現暱稱被擋住、TRIP 被截斷、換行行為不符合預期等顯示問題。需求為保留單行 TRIP，不換行，並讓暱稱/TRIP 欄位比例更平衡。
- 重現步驟：
	1. 進入回放或 preview 房間頁
	2. 觀察第二列暱稱與 TRIP 區塊
	3. 在長暱稱或長 TRIP 下，會出現截斷與遮擋感
- 影響範圍： [前端] / 後端 / 規則 / 資料同步
- 優先級： P0 / [P1] / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：preview 卡片的第二列欄位比例與 TRIP 文本規則在多次調整後不一致，導致暱稱與 TRIP 可用寬度分配失衡。
	- 修正方式：已在 `frontend/src/theme.css` 的 room preview 區段統一調整：
	  1. `grid-template-columns` 改為 `minmax(0, 1.15fr) minmax(0, 0.85fr)`
	  2. 取消 TRIP 偏移（`padding-left` 與菱形額外 `margin-left`）
	  3. TRIP 強制單行不換行，保留省略號截斷（`white-space: nowrap; text-overflow: ellipsis; overflow: hidden;`）
	- 驗證結果：`frontend/src/theme.css` 錯誤檢查通過；待頁面實機確認長暱稱與長 TRIP 的最終視覺是否符合預期。

### 顯示修正 / BUG / 規則確認 / [UX]
- 問題標題：如果因為貪婪卡片需要把裝備給其他人，請自動彈出裝備選擇。
- 問題說明：目前如果收到貪婪的綠卡後，確認完卡片原本要求玩家再點擊自己的玩家卡片選擇裝備給予其他人，這個流程可以簡化，變成玩家確認完綠卡內容後，由系統自動點擊玩家卡片彈出裝備選擇。請確認是否有其他類似流程可以簡化。
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / [P1] / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：原本 pending_steal 流程依賴玩家再點一次來源玩家卡，操作多一步且容易誤以為卡住。
	- 修正方式：前端已在 `card_effect` / `Erstwhile Altar` 結算後自動呼叫 `maybeResolvePendingSteal()`；只要後端回傳 `pending_steal`，就會直接彈出裝備選擇 dialog，不需要再點一次自己或來源玩家卡。
	- 驗證結果：已確認程式碼入口存在於 `frontend/src/pages/room.js`，待實機確認貪婪與祭壇兩條流程都能自動彈窗。
	- 備註：此項先前已落地，但 issue 尚未回填；可一併檢查是否還有其他 `pending_*` 流程適合自動開窗。

### 顯示修正 / [BUG] / 規則確認 / UX
- 問題標題：531村 貪婪綠卡結算邏輯BUG  
- 問題說明：A對B使用貪婪綠卡，但綠卡還沒結算，A就可以點選攻擊。攻擊完後B才結算綠卡。結算後變成A的下家C的攻擊回合，跳過了A的傷害→結束階段和C的開始→移動→場地階段。C攻擊D沒有觸發搶奪裝備流程，一直卡在攻擊階段最後暴斃。暴斃後D的裝備也沒有丟進棄牌區。
- 重現步驟：
	1. 多人遊戲中，玩家A於status=3時抽到貪婪綠卡
	2. A指定玩家B為目標，設置 _pending_green_card
	3. 在B確認之前，A點選「攻擊」按鈕（應被禁止但未禁止）
	4. A執行攻擊後，B才結算綠卡
	5. 綠卡結算後直接變成C的攻擊回合，跳過中間階段
- 影響範圍： 後端 / 規則邏輯
- 優先級： [P0] / P1 / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：在 `next_step()` 的 status=4（攻擊階段）邏輯中缺少對 `_pending_green_card` 的檢查；以及 `steal_equipment()` 對輸入參數的驗證不足
	- 修正方式：
	  1. 在 status=4 進入攻擊流程前加入檢查：若 `self._pending_green_card` 存在則 return，禁止綠卡結算前的攻擊
	  2. 強化 `steal_equipment()` 的參數驗證，確保 from_player/to_player/equipment 皆有效，且 equipment 確實在 from_player 的裝備清單中
	- 驗證結果：語法檢查通過，邏輯修復已實施
	- 備註：修改檔案 `backend/game/room.py` 共2處；測試程式 `test_artifacts/test_greed_bug_scenario.py` 驗證基礎邏輯；需透過 20+ 局實機測試驗證回合流程完整性

### [顯示修正] / BUG / 規則確認 / UX
- 問題標題：系統訊息與房間資訊在地化
- 問題說明：目前系統資訊跟村莊設定(初始綠卡跟暴斃時間)都顯示中文，沒有轉換成對應的語言。修改房間設定按鈕也是(這個似乎只有英文版沒有顯示對應語言)。
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / [P1] / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：房間資訊摘要與系統訊息顯示層都有硬編碼文案；除了後端送出的中文原文之外，`room.js` / `room_preview.js` 也會再把常見系統訊息重新組裝成中文句子。
	- 修正方式：在 `frontend/src/locales/zh.js`、`en.js`、`jp.js` 補上 `room.system.*` 共 38 個 i18n 鍵值（含高頻系統訊息 + 移動/抽牌/使用卡/效果/戰鬥/裝備/死亡等完整戰鬥訊息），並將 `room.js` / `room_preview.js` 的 `resolveCardEffectSource()` 與 `formatSystem()` 內所有中文硬編碼模板字串全部替換為 `t('room.system.*', {...})` 呼叫。
	- 驗證結果：相關前端檔案語法錯誤檢查通過，格式搜尋確認無殘留中文模板字串。

### 顯示修正 / BUG / 規則確認 / [UX]
- 問題標題：村長的功能表內多一個確定鍵
- 問題說明：現在每更改一個選項都會提示一次，應該改成可以一次設定完後按確認生效。要注意保持卡組選擇防呆互動。
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / [P1] / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：房間設定 dialog 原本綁在 `change` 事件上，每改一個欄位就立即送出 `update_room_settings`。
	- 修正方式：已改為保留卡組防呆，但不再於 `change` 時送出；新增 dialog 內的「確認設定」按鈕，按下後才一次送出 `expansion_mode`、`turn_timeout_minutes`、`enable_initial_green_card`。
	- 驗證結果：`frontend/src/pages/room.js` 無錯誤；待在實際村長操作流程驗證「多欄位修改後一次生效」。
	- 備註：目前此修改已在 `room.js` 落地；若實際使用頁面仍有另一套 room preview 腳本，需再確認是否要同步相同行為。

### 顯示修正 / [BUG] / 規則確認 / UX
- 問題標題：更換玩家代表色塊功能仍然遺失
- 問題說明：現在加入村子後，還是不能點色塊更換顏色
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： [P0] / P1 / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：後端 `change_color` API 與前端 dispatch 其實都還存在，但玩家卡上的改色入口使用 `details/summary` 色塊控制，互動過於隱性，且在目前房間版面下容易被誤認為不可點或在部分瀏覽器行為不穩。
	- 修正方式：已將 `frontend/src/ui.js` 的代表色入口改為明確的按鈕式 color picker，點一下展開色盤，再點顏色送出 `change_color`；同時補強 `click` 的 `preventDefault/stopPropagation`，並在 `frontend/src/theme.css` 調整 focus / hover / 彈出面板樣式，讓招募階段自己的色塊有清楚可操作性。
	- 驗證結果：相關前端檔案錯誤檢查通過；待實際進房驗證招募中、未準備狀態下可正常展開並換色。
	- 備註：後端限制仍維持原本規則，只允許招募中且未準備的本人更換顏色。

### [顯示修正] / BUG / 規則確認 / UX
- 問題標題：頭像登錄上傳按鈕多語言顯示
- 問題說明：頭像上傳這邊，選擇檔案的按鈕跟未選擇檔案的提示都是中文。
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / P1 / [P2]
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：瀏覽器原生 `input[type=file]` 會顯示作業系統語言的預設按鈕與「未選擇檔案」文字，導致頁面切到英文或日文時仍殘留中文。
	- 修正方式：`frontend/avatar.html` 改為隱藏原生 file input，改用自訂按鈕與檔名標籤；`frontend/src/pages/avatarGallery.js` 新增同步檔名顯示與重設行為；`frontend/src/locales/zh.js`、`en.js`、`jp.js` 補上 `avatar.choose_file`、`avatar.no_file`。
	- 驗證結果：相關 HTML / JS / locale 檔案錯誤檢查通過；待實際切換語言確認按鈕與未選檔提示都會跟著更新。
	- 備註：這次做法也避免不同瀏覽器對原生 file input 文案不可控的問題。

### [顯示修正] / BUG / 規則確認 / UX
- 問題標題：身分登錄多語言顯示
- 問題說明：身分登錄裡面，TRIP一覽，TRIP個人紀錄這些分頁標籤沒有翻譯，裡面搜尋的提示跟按鈕也都沒有翻譯。表格的標題也沒有翻譯
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / P1 / [P2]
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：`frontend/resident_register.html` 仍保留大量硬編碼中文標籤、placeholder、表格欄位與 dialog 文字，`register.js` 也殘留中文錯誤 toast。
	- 修正方式：已將身分頁分頁標籤、搜尋區、表格標題、上一頁/下一頁按鈕、管理表單 placeholder、評級說明 dialog 等改用 `data-i18n` / `data-i18n-placeholder` / `data-i18n-aria`；並將 `register.js` 的管理中 TRIP 狀態與載入失敗訊息改為 locale key。
	- 驗證結果：相關 HTML / JS / locale 檔案錯誤檢查通過；待切換英日語確認完整畫面文案與 toast 均正確。
	- 備註：這次一起補上 `managed_trip_current`、`skill_level_title_with_level` 等缺漏鍵值，避免局部仍回退成中文。

### [顯示修正] / BUG / 規則確認 / UX
- 問題標題：紀錄閱覽裡的徽章說明修正
- 問題說明：把各徽章的說明標題"B徽章"、"G徽章"這個拿掉，只留下"初始綠卡"，"基本卡組"。
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / [P1] / P2
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：徽章 popover 標題仍使用 `G 徽章` / `B 徽章` / `E 徽章` 命名，不符合目前顯示需求。
	- 修正方式：已更新 `frontend/src/locales/zh.js`、`en.js`、`jp.js` 的 `records.badge_*_title` 與對應說明文字，改為只顯示「初始綠卡 / 基本卡組 / 擴充卡組」等實際項目名稱。
	- 驗證結果：locale 檔案錯誤檢查通過；待在紀錄頁實際點擊徽章確認 popover 文案。
	- 備註：這次只動文案，不影響徽章代碼 `G/B/E` 本身的識別與渲染。

### [顯示修正] / BUG / 規則確認 / UX
- 問題標題：操作流程說明更新
- 問題說明：
1. 把第7點踢人投票流程移到第二點準備與開局前(注意流程的編號要跟著改變)
2. 準備與開局的說明，應該說明全員準備完成且人數達標(4人)遊戲會自動開始，還有說明村長能夠更改遊戲設定跟點名
3. 踢人流程要說明達標是3票
4. 回合推進裡面，把"也可使用"改成"或者使用"
- 重現步驟：
	1.
	2.
	3.
- 影響範圍： 前端 / 後端 / 規則 / 資料同步
- 優先級： P0 / P1 / [P2]
- 修正說明：
	- 現況： 未處理 / 修正中 / [已修正] / 待驗證
	- 原因：`operation.html` 與多語系 `operation.step*` 文案仍是舊順序與舊描述，未反映踢人投票流程前移、自動開局條件與最新用詞。
	- 修正方式：已在 `frontend/operation.html` 將踢人投票流程移到第 2 點，並同步更新 `frontend/src/locales/zh.js`、`en.js`、`jp.js`：補上 3 票踢人門檻、村長可調設定與點名、4 人且全員準備後自動開局，且把回合推進文案改成「或者使用 / or / または」。
	- 驗證結果：相關 HTML / locale 檔案錯誤檢查通過；待頁面實際切語言確認排序與文案一致。
	- 備註：此項目前是內容與順序都已落地，沒有牽動遊戲邏輯。
